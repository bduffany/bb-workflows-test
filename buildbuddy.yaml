actions:
  - name: Build and test (M1 Mac)
    os: darwin
    arch: arm64
    triggers:
      push: { branches: [ main ] }
      pull_request: { branches: [ main ] }
    bazel_commands:
      - test //... --nocache_test_results
